package com.example.foody.builder;

import com.example.foody.model.user.AdminUser;

/**
 * Interface for building AdminUser objects.
 * Extends the {@link UserBuilder} interface with AdminUser as the type parameter.
 */
public interface AdminUserBuilder extends UserBuilder<AdminUser> {
}